<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Form PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Form name="HDS-ServiceAccountManagement" type="Workflow">
  <Attributes>
    <Map>
      <entry key="isWizard" value="true"/>
      <entry key="pageTitle" value="CreateDetails"/>
      <entry key="title" value="Details"/>
      <entry key="width" value="300"/>
    </Map>
  </Attributes>
  <Section columns="3" name="dataCollection">
    <Field columnSpan="1" displayName="Prefix" name="prefix" postBack="true" required="true" type="string">
      <Script>
        <Source><![CDATA[
          return "srvc";
        ]]></Source>
      </Script>
    </Field>
    <Field columnSpan="1" displayName="SAMAccountName" name="sAMAccountName" postBack="true" required="true" type="string">
      <ValidationScript>
        <Source><![CDATA[
          import sailpoint.object.Identity;
          import sailpoint.api.IdentityService;
          import sailpoint.object.Application;
          import sailpoint.object.Link;
          import sailpoint.tools.Message;

          Message msg = new Message();

          Identity identity=context.getObjectById(Identity.class,value);
          IdentityService identityService = new IdentityService(context);

          if(identity!=null){

          if(secondaryIdType.equalsIgnoreCase("ADAdmin")){
          Application adApp=context.getObjectByName(Application.class,"ActiveDirectory");

          if(adApp!=null){
          List adLinks=identityService.getLinks(identity,adApp);

          if(adLinks!=null @and !adLinks.isEmpty()){
          return null;
          }else{
          msg.setKey("Identity doesn't have Account in AD select existed identity");
          }
          }
          }
          else if(secondaryIdType.equalsIgnoreCase("CloudAdmin"))
          {
          Application ldapApp=context.getObjectByName(Application.class,"LDAPApplication");
          if(ldapApp!=null){
          List ldapLinks=identityService.getLinks(identity,ldapApp);

          if(ldapLinks!=null @and !ldapLinks.isEmpty()){
          return null;

          }else{
          msg.setKey("Identity doesn't have Account in LDAP select existed identity");
          }
          }
          }
          }

          return msg;
        ]]></Source>
      </ValidationScript>
    </Field>
    <Field columnSpan="1" displayName="GivenName" name="givenName" postBack="true" required="true" type="string"/>
    <Field columnSpan="1" displayName="DistinguishedName" name="distinguishedName" postBack="true" required="true" type="string"/>
    <Field columnSpan="1" displayName="Mail" name="mail" postBack="true" required="true" type="string"/>
    <Field columnSpan="1" displayName="CN" name="cn" postBack="true" required="true" type="string"/>
  </Section>
  <Button action="back" label="Cancel" value="Cancel"/>
  <Button action="next" label="Submit" value="Submit"/>
</Form>